# Signal Worker

Responsibilities:
- evaluate promoted strategy versions against current candles
- emit dedupe-safe `signal_events`
- keep local Python evaluation as the primary v1 signal source
- treat TradingView alerts as an optional adapter, not the core runtime

Not yet implemented. This folder is scaffold only.
