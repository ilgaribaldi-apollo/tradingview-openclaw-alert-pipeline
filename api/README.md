# API + Contract Foundation

This directory defines the boundaries between:
- Python workers
- Next.js operator views
- optional inbound webhook adapters

No webhook endpoint or runtime API is live yet. These files exist so future implementation can share one contract vocabulary.
