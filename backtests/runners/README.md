# Backtest Runners

Put runner scripts or runner-specific notes here.

The first implementation should:
- accept a stored config
- point at a strategy wrapper or translated implementation
- produce metrics, trades, and summary artifacts under `../../results/runs/<run-id>/`

Keep the interface boring and repeatable.
